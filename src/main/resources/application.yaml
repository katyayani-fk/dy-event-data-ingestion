
server:
  port: 5050
logging:
  level:
    root: INFO
    com:
      fourkites:
        facilities:
          dy:
            EventDataIngestion:
              controller: DEBUG
              service: TRACE
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/yi_app_db
    username: newt
    password: Iengoa0iCh3ahd4oe2ch
    hikari:
      maxPoolSize: 10
      poolName: DYLocal


